# CoreSim JSBSim Test Data

This directory contains a project-authored JSBSim placeholder model for CoreSim
adapter and repeatability tests.

The `flying_placeholder` model is infrastructure-only. It is not a production
aircraft, does not represent a named aircraft type, and must not be used as
validation evidence for aircraft fidelity claims.
